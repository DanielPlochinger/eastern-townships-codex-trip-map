# Eastern Townships Trip Map

Static, GitHub Pages-friendly mobile map built from `eastern_townships_activities_v3.md`.

Open `index.html` through a local server or GitHub Pages. The app uses Leaflet with OpenStreetMap tiles, shows every guide item as a pin, includes compact category filters, supports browser geolocation on secure origins, and links each item back to Google Maps.
